---
name: pelican-bench
description: "让若干条「模型 x 通道」画同一张「鹈鹕骑自行车」SVG 并出并排看板，同时拿一段固定文本量出每条通道的 input token 注入、思考 token 抑制和响应元数据指纹。适用于怀疑 API 中转层降级、偷塞隐藏 system prompt 或关掉思考，也适用于单纯横评几家模型在同一道题上的出图能力；只判定通道完整性，不负责判定端点背后到底是哪个模型（那是行为指纹类工具的事）。"
---

# 鹈鹕横评（Pelican Bench）

「生成一张鹈鹕骑自行车的 SVG」是 Simon Willison 2024 年起的一个非正式基准：场景在训练数据里
几乎不存在，模型只能真的去算几何，不能靠背。本技能把它从「看个乐」变成可用的诊断：

- **图**是感官参照 —— 同一道题，各家画成什么样，一眼可比。
- **通道体检**才是实锤 —— 每轮先拿一段逐字不变的短文本打一发，只看服务端自己报回来的
  `prompt_tokens`、响应 id、`usage` 字段。这些是数字，不是观感。

## 什么时候用

- 用了 API 中转 / 网关 / 转售商，怀疑拿到的不是宣称的东西
- 换了供应商，想在投产前验一验行为有没有变
- 想横评几家模型在同一道题上的表现

**不适用**：判定「这个端点背后到底是哪个模型」。那是模型身份问题，行为指纹类工具（单 token
答案分布 + Jensen-Shannon 散度）更合适。本技能量的是**通道完整性**——请求在路上被改了什么。
两者互补：指纹工具的已知弱点是「只换 system prompt 造成的散度漂移，跟换模型一个量级」，
分不清是哪种；而本技能正好能把注入量直接读成一个 token 数。

## 装与跑

只用 Python 标准库，不装依赖。

```bash
cp scripts/lanes.env.example ~/.config/pelican-bench/lanes.env
chmod 600 ~/.config/pelican-bench/lanes.env   # 里面是密钥
$EDITOR ~/.config/pelican-bench/lanes.env     # 填端点、密钥、模型

python3 scripts/pelican_bench.py --open       # 跑一轮并打开看板
python3 scripts/pelican_bench.py render       # 只用已有快照重出看板
```

可选参数：`--config PATH`（泳道配置）、`--out DIR`（快照与看板目录）、`--keep N`（保留最近 N 轮）。

想常态化就挂个定时器，半小时一轮足够。脚本自带锁，上一轮没跑完不会叠上来。

## 泳道怎么配才有意义

**同一个模型至少配两条通道：可信的官方直连 + 待验的中转。** 官方直连是基线；没有基线，
所有数字都只是绝对值，看不出被动了什么手脚。大多数同类工具的难处也在这 —— 它们得靠
社区维护的参考指纹，而你手里往往真有直连账号可比。

字段说明和一份可直接改的模板见 `scripts/lanes.env.example`。**泳道名和行序都从配置推**，
按 `PB_<名>_BASE_URL` 出现的先后排，加泳道只改配置文件、脚本不动；想自己定顺序就加一行
`PB_LANE_ORDER=名1,名2,名3`。

## 结果怎么读

| 信号 | 含义 |
| --- | --- |
| 入 token 比同款模型的直连**多** | 中转往每个请求里塞了隐藏上下文。**恒定**差值 = 固定的 system prompt；**忽大忽小** = 后面挂着多个池子、路由随机，意味着同一个 prompt 的行为不可复现 |
| 入 token 比直连**少** | 上下文被截断了，长输入场景要特别当心 |
| 思考 token 为 0 或远低于直连 | 同一个思考档位下推理被抑制。**先确认它会回传这个字段**：回传通的通道数字才可信，恒 0 的要再验一次是不是压根不回传（见下） |
| `usage` 里冒出非标准字段 | 中转层的实现指纹。第三方转售常会漏出它自己的计费、缓存或上游平台字段 |
| 响应 id 前缀不对 | Anthropic 真实 id 以 `msg_01` 开头，OpenAI 是 `chatcmpl-`。给出别的前缀说明响应被改写或伪造 |
| `model` 回显跟请求不一致 | 直接证据，虽然容易伪造 |
| 判「没画出来」 | 两种原因：SVG 不是合法 XML（`<img>` 走严格 XML 解析，属性重复就整张白屏），或用了 `<image>` / `<text>` / 外部 `href` 这类绕过作图的手段 |

图元数（`path_count`）只是粗糙的复杂度指标，好不好看仍需人眼。

### 思考 token 恒为 0 时要再验一步

「没思考」和「不回传思考计数」是两回事，别把后者报成前者。判别方法：

1. 同一个端点，分别用 `reasoning_effort: "low"` 和 `"high"` 各打一发。数字跟着变 → 回传是通的，
   那么低数值就是真实的思考量。
2. 如果该模型有原生协议（如 Anthropic 的 `/v1/messages`），换协议再打一发，看响应里有没有
   thinking 内容块。有块无计数 = 只是不回传；无块 = 真的没思考。

## 方法上的坑

- **题面逐字不能改**。`Generate an SVG of a pelican riding a bicycle.` 是通行基准，满世界的
  对比图都用它，改了就没法跟公开语料横着比。作者 2025-11 发过一版更严的（指定加州褐鹈鹕、
  辐条、繁殖期羽色），区分度确实更高，但没成为通行口径 —— 流传的图基本都是白鹈鹕，说明
  大家用的还是原版。本技能保留原版。
- **体检用的固定文本同样逐字不能改**。input token 数就是拿它当尺子量的，改一个字，历史数据
  全部作废。
- **各家的思考旋钮对不齐**：OpenAI / DeepSeek 用 `reasoning_effort`，部分国产模型用
  `enable_thinking` + `thinking_budget`，Anthropic 某些模型只接受 `thinking.type=adaptive`
  且**没有档位可调**。做不到严格对齐就别假装对齐 —— 看板逐条标注档位、单列思考 token，
  让差距可见，而不是藏在总 token 里。
- **必须流式**。非流式下 reasoning 模型会思考几分钟一个字不吐，连接直接读超时。
- **成本**：每条泳道每轮两次调用（体检 + 画图），画图那次可能吃掉上万 token。泳道多了要算账。
- **延迟数字会被本机负载污染**，它量的是「这台机器打过去多久回来」。跨通道比较优先看
  token 数和元数据，那些是服务端算的；延迟当参考。

## 单轮结论不要当定论

一次异常可能是抖动。**判「恒定注入」还是「间歇注入」，至少要三轮以上**，而这恰好是最有价值的
区分：恒定的可以当常量扣掉，间歇的意味着路由随机、行为不可复现。

脚本默认只留最近 3 轮，要做长期趋势，先把每轮的 `run.json` 归档出去。字段含义见
[原始数据说明](references/raw-data.md)。
